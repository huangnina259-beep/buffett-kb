[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python src/ingest_md.py --force && uvicorn server:app --host 0.0.0.0 --port 8000"

[[volumes]]
mountPath = "/app/database"
